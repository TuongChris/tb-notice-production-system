# Current engineering state

Observed project engineering state only. Not legal, canonical or case evidence; it does not supersede Drive records, the specifications or the frozen references. Updated at the P4D R11 submission on `feature/p4d-production-context` (2026-09-25 UTC, home PC = the first PC of the P0–P4D records).

| Item | State |
|---|---|
| Branches | `bootstrap/p0-local` — the P0 branch at the P0 reproduction baseline; it takes **no P1 application code**. `feature/p1-auth-shell` — P1/P1.1, accepted at R4.1, head `8fe96ae`, unchanged since. `feature/p2-directory` — P2 directory work and its R5 closeout, head `31db581`, branched from the accepted P1.1 head `8fe96aea5a30bb01fa24c987e30689f4e6532164`, unchanged since. `feature/p3a-sources-route` — P3A, branched from the R5 closeout head `31db581`, accepted head `c1b8d22`, unchanged since; merged into `main` by pull request #1 (merge commit `adea2bc`). `feature/p3b-representation-authority` — P3B, created from `adea2bc`; it holds the R6 closeout (`c8da59f`), the P3B implementation (`9e7e6f6`, `f79aa0a`, `be5cb4c`, `e23da02`, `04b8528`), the R7 submission documentation (`8e513f0`), the R7 remediation `ee31fa3` and its documentation; accepted head `05ce7b0`, unchanged since; merged into `main` by pull request #2 (merge commit `3649bef`, merged by the repository owner 2026-09-24T13:50:21Z; parents `adea2bc` and `05ce7b0`; its tree is identical to `05ce7b0`). `feature/p4a-case-core` — created from that exact `3649bef` for P4A; it holds the R7 closeout records (`b97ac13`), the lint fix `d615297`, the P4A implementation (`2cae729`, `119c45b`, `b6aeeac`, `61164fa`, `6330aeb`) and its documentation (`7a11ce4`, the R8 submission head), then the R8 remediation (`2d74b5a`, `b6be5c9`, `e745a20`, `ab56139`) and its documentation (`eeb94ce`, the R8 final submission head), then the R8 final closeout record `602f0da` (the accepted closeout head); **R8 final = PASS**; merged into `main` by pull request #3, unchanged since. `feature/p4b-case-intake` — created from the P4A merge commit `1d91c41ec480aed3579122e86485df1e73fbb741` (pull request #3, merged 2026-09-25T04:26:08Z; parents `3649bef` and `602f0da`) for P4B; it holds the post-merge R8 checkpoint record (`e77f809`), the P4B implementation (`99e6907`, `b3a948c`, `e2ce11d`, `64c33d4`, `aa9ee2c`, `f952b73`, `900c0ea`, `35cf552`) and its R9 documentation (`6ff6775`, the R9 submission head); **R9 = PASS_WITH_ONE_CONTRACT_REMEDIATION**; merged into `main` by the operator with pull request #4, unchanged since. `feature/r9-fact-support-readback` — created from the P4B merge commit `eb83b19d224c0c7e6bf1bd7de5c21bd3c2e0c361` (pull request #4, merged by the operator 2026-09-25T07:16:46Z; parents `1d91c41` and `6ff6775`; its tree is identical to `6ff6775`) for the R9 remediation; it holds the contract release TB-SCHEMA-API-v1.2.0 (`fbcd8ae`), the API read (`02332da`), the UI (`b862cf7`) and its documentation (`a31ad90`, the accepted R9-final head); **R9 final = PASS**; merged into `main` by the operator with pull request #5, unchanged since. `feature/p4c-correspondence` — created from the R9 remediation merge commit `d2b6f0030f9b7e53b612eaf1fa6dffffb98e4395` (pull request #5, merged by the operator 2026-09-25T10:58:02Z; parents `eb83b19` and `a31ad90`; its tree is identical to `a31ad90`) for P4C; it holds the R9 closeout checkpoint record (`abad3ee`), the P4C implementation (`fa4f98e`, `9514769`, `48ceebd`, `0038c56`, `b9fcaf2`), its R10 documentation (`1b2e27b`, the R10 submission head, and `d6cd152`, the accepted head) and the R10 closeout record (`edebb80`, the closeout head); **R10 = PASS**; merged into `main` with pull request #6, unchanged since. `main` — at `ed5b3d743c8c64dc1c54b38e2e13bd9aa80d5921`, the merge commit of pull request #6 (merged 2026-09-25T15:17:05Z; parents `d2b6f00` and `edebb80`; its tree is identical to `edebb80`); the local `main` was fast-forwarded to it. `feature/p4d-production-context` — created from that exact `main` head for P4D; it holds the post-R10 checkpoint record (`ca3d7cd`), the P4D implementation (`75f07c2`, `8206bbf`, `c7df48b`, `6bbba3a`, `bfeb9f1`, `1b012bd` — the code head) and its R11 documentation (the R11 submission head); **R11 = PENDING**; no pull request, not merged |
| Completed | P0-A, P0-B, P0-C1–C7, P0-D, P0-E (first PC and CI); P0 Windows-browser checkpoint (operator-reported); P1 authentication + application shell (R4 **PASS_WITH_NOTES**, 2026-09-23; `docs/verification/p1/P1_AUTH_SHELL.md`); P1.1 local recovery commands (R4.1 **ACCEPTED**, operator, 2026-09-24; `docs/verification/p1/P1_1_AUTH_RECOVERY.md`); P2 Directory (R5 **PASS_WITH_ONE_REMEDIATION**, operator, 2026-09-24; the identity-lock remediation is implemented and verified — `docs/verification/p2/P2_DIRECTORY.md` §15; R5 then closed by the operator: "R5 review result: PASS", `P2_STATUS = VERIFIED_COMPLETE_FOR_CURRENT_SCOPE`); P3A Sources, canonical bindings and Route (R6 **PASS**, operator, 2026-09-24; `P3A = VERIFIED_COMPLETE`, `MERGED_TO_MAIN` — `docs/verification/p3a/P3A_SOURCES_ROUTE.md` §19); P3B Representation authority (R7 **PASS_WITH_ONE_REMEDIATION**, then **PASS** at the closeout, operator, 2026-09-24; `P3B = VERIFIED_COMPLETE`, `MERGED_TO_MAIN` — `docs/verification/p3b/P3B_REPRESENTATION_AUTHORITY.md` §21–§22); P4A Case core and case authority selection (R8 **PASS_WITH_ONE_CONTRACT_REMEDIATION**, then **PASS** at R8 final, operator, 2026-09-25; `P4A = VERIFIED_COMPLETE`, `MERGED_TO_MAIN` — `docs/verification/p4a/P4A_CASE_CORE.md` §21–§23); P4B Case intake material (R9 **PASS_WITH_ONE_CONTRACT_REMEDIATION**, then **PASS** at R9 final, operator, 2026-09-25; `P4B = VERIFIED_COMPLETE`, `MERGED_TO_MAIN` — `docs/verification/p4b/P4B_CASE_INTAKE.md` §24–§26); P4C Correspondence (R10 **PASS**, operator, 2026-09-25; `P4C = VERIFIED_COMPLETE`, `MERGED_TO_MAIN` — `docs/verification/p4c/P4C_CORRESPONDENCE.md` §29–§30) |
| Current mission | TB_R10_CLOSEOUT_MERGE_AND_P4D_PRODUCTION_CONTEXT_TO_R11 (operator, 2026-09-25) — **R10 = PASS**: P4C = VERIFIED_COMPLETE (accepted head `d6cd152`, final CI run 36141175731 success, both jobs); the active wire contract stays TB-SCHEMA-API-v1.2.0. Done: the R10 closeout record `edebb80` (push run 36151228476 success); pull request #6 merged with a normal merge commit `ed5b3d7` (pull_request run 36152011037 success); post-merge `main` CI run 36153127772 success, both jobs (`P4C_CORRESPONDENCE.md` §29–§30); the branch `feature/p4d-production-context` from the exact `main` `ed5b3d7`; the post-R10 checkpoint `ca3d7cd`; **P4D — Production context** (`getProductionContext` only, read-only) implemented (code head `1b012bd`) and verified: automated suites, consistent-snapshot test, browser 32/32, negative controls 22/22, full regression 21/21 (`P4D_PRODUCTION_CONTEXT.md`). Submitted for review gate **R11 — PENDING**: no P4D pull request or merge; no prompts, PromptSnapshot, candidates, validation, assessments, readiness or external action |
| Development topology (ADR-0003) | `PRIMARY_DEVELOPMENT_WORKSTATION = HOME_PC` (the first PC). The second-PC reproduction is **DEFERRED_BY_OPERATOR**; the runbook is preserved and stays valid for any future workstation. GitHub is the source of truth for code, branches, committed migrations and CI verification; local database volumes are never copied or synchronized through Git |
| R4 decisions (accepted) | Frozen-contract status mapping kept (403 INVALID_CREDENTIALS, 400 login validation/oversized public request); both exact dev origins kept — canonical Windows-browser origin `http://localhost:5173`, `http://127.0.0.1:5173` for WSL tools, cookies are per hostname and never shared; 12 h absolute / 30 min idle; password policy 15–256 code points, NFKC, current Argon2id; session-row retention cleanup DEFERRED (not blocking P2) |
| P2 scope decisions (operator, mission TB_P2_DIRECTORY_TO_R5) | D1 OwnerSubject included; D2 operations needing a SourceReference that the product cannot yet author are DEFERRED to the Source phase (no placeholder sources); D3 "established" Agency/LegalSubject = ACTIVE, canonically bound or referenced by another persisted record; D4 fieldAttributions checks; D5 hard delete only for unused, unbound DRAFT rows; D6 minimal protected Directory pages |
| R5 decisions (operator, 2026-09-24) | Accepted: A restore returns an archived record to DRAFT; B archived records are read-only except the explicit restore; C Signer administrative state carries no legal-authority meaning (no mandate coverage, eligibility, G7, signature authority or notice adoption); D an agency-less SourceReference satisfies an Agency/Signer reference only when explicitly scoped to that agency; E accent-insensitive matching only for discovery search, never for identity equality, canonical matching, duplicate determination, unique keys or legal/entity equivalence. Overridden and remediated: once established, **every** identity-defining field is locked against generic PATCH, empty ones included; a future correction/supplement workflow is not invented |
| R6 decisions (operator, 2026-09-24) | Accepted: 1 a canonical binding requires a current CANONICAL_RECORD SourceReference; 2 a canonical binding cannot be replaced through the current workflow — correction needs a future reconciliation workflow; 3 a source revision keeps its owning Agency and scope; 4 a default Signer cannot be ENDED; 5 Route restore/relink is refused while a participating party is archived; 6 a canonically bound Signer's full legal name is identity-locked; 7 Owner source scope stays conservatively enforced against reuse of another Owner's material until a more explicit owner-scope model exists; 8 the source-aware P2 field-attribution/link validation stays active; 9 creating an Agency-owned SourceReference counts as a persisted reference and so establishes/locks that Agency under D3; 10 new operation-specific error codes only inside the existing free-string error `code` field, with the contracted HTTP statuses and response shapes. No contract change is authorized by this acceptance |
| R7 decisions (operator, 2026-09-24) | Accepted: 1 a Mandate has no established-identity lock (a representation/appointment container, not a LegalSubject identity record); 2 an archived Mandate stays read-only, including no new AuthorityEvent — a historical or retroactive event is recorded by restoring the Mandate administratively, recording the event with its actual supplied effective/occurred date and archiving again if appropriate; `recordedAt` is never substituted for `effectiveAt`; 3 UNTIL_TERMINATED plus an `expiresOn` may be preserved as supplied documentary facts, with no currentness, expiry or contradiction resolution inferred; 4 a PAUSED Route and a PAUSED Signer may take part in P3B documentary records where the contract allows (administrative state; proves or disproves no legal authority); 5 a truthful incomplete DRAFT MandateVersion may be frozen (FROZEN = immutable snapshot only — not complete, legally approved, current authority, G1 PASS, owner-confirmed or signer-adopted); 6 `Route.preferredCoverageId` is not cleared automatically when its Mandate is later archived — the recorded preference is kept and any later Case/authority-selection workflow must revalidate the Coverage; 7 the conservative owner-material source-scope extension stays until a more explicit owner-scope model exists; 8 permanent: Coverage ≠ G1 PASS, CoverageSigner ≠ G7, an AuthorityEvent's existence is not proof by itself, an application User is not a Signer. Required remediation: SourceReference timestamp storability (done, `ee31fa3`); R7 then closed as **PASS**. No contract amendment is authorized |
| DOCUMENT_REVIEWED (permanent rule) | A SourceReference row does not itself establish DOCUMENT_REVIEWED. DOCUMENT_REVIEWED means an actual human review of the document occurred; the system records that provenance only from an explicit, supported, human-entered fact representing that real review. A reviewer name, URL, source row, hash or canonical binding never by itself upgrades provenance to DOCUMENT_REVIEWED; defaults remain non-upgrading |
| P0 reproduction baseline | `P0_REPRODUCTION_BASELINE` = `b9eea3755a87490636cbb0f2e7aec1a59e15d64c` (documentation-only Windows-browser checkpoint; branch CI run `35873152877` success). `feature/p1-auth-shell` branches from it; it remains the commit named for the deferred second-PC reproduction (ADR-0003) |
| Not started | The later case phases (prompts, candidates, validation, assessments, readiness, unsigned export) and every later phase — each needs an explicit approved mission (P4D production context is implemented and submitted for R11) |
| External legal actions | 0 |
| Real case data | 0 (synthetic fixtures only) |
| Accounts | No real application account exists in any committed or CI artefact. The operator manages local accounts with `yarn admin:create`, `admin:password`, `admin:disable`, `admin:enable` and `admin:revoke-sessions` (own passwords; never committed). CI creates and recovers only a synthetic account in its disposable database |
| Deployment | none |
| Latest CI-verified code commit | P4D on `feature/p4d-production-context`: the keyboard fix `6bbba3ac0477c0afb929d926bc24afc654b804b9` (push run `36165581902`, both jobs success; `yarn test` 1397 in 41 files, `yarn test:db` 422 in 11 files, `smoke:p4d` 88 checks, `smoke:local` 48, lint "Found 0 warnings and 0 errors.", entry chunk 325.73 kB with no bundle advisory); the compiled smoke `c7df48b` (push run `36162478106`, `smoke:p4d` 88 checks); the API `75f07c2` (push run `36160776299`). The later test-only commits `bfeb9f1` and `1b012bd` pass the full home-PC regression and are pushed with the R11 documentation, whose run is reported at R11. `main` after the P4C merge: `ed5b3d743c8c64dc1c54b38e2e13bd9aa80d5921` (push run `36153127772`, both jobs success; `yarn test` 1377 in 39 files, `yarn test:db` 380 in 10 files, `smoke:p4c` 62 checks, lint "Found 0 warnings and 0 errors.", entry chunk 323.25 kB with no bundle advisory). Accepted P4C closeout head `edebb80` (push run `36151228476`; pull_request run `36152011037`; both success). P4C accepted head `d6cd152e27a6ecb51c1038ebddcfe3af2ac1139e` (documentation on the code head `b9fcaf2`; push run `36141175731`, both jobs success; `yarn test` 1377 in 39 files, `yarn test:db` 380 in 10 files, `smoke:p4c` 62 checks, lint "Found 0 warnings and 0 errors."); the R10 submission head `1b2e27b` (push run `36140515842`, both jobs success). P4C on `feature/p4c-correspondence`: the code head `b9fcaf2924d268b4c2b2d5b726e2e576cf330afa` (push run `36139089683`, both jobs success; `yarn test` 1377 in 39 files, `yarn test:db` 380 in 10 files, `smoke:p4c` 62 checks, `smoke:local` 46, lint "Found 0 warnings and 0 errors.", entry chunk 323.25 kB with no bundle advisory); the UI `0038c56` (push run `36135445880`); the API with the new compiled `smoke:p4c` `48ceebd` (push run `36133062817`, `smoke:p4c` 62 checks); the R9 closeout checkpoint `abad3ee` (push run `36129721720`). `main` after the R9 remediation merge: `d2b6f0030f9b7e53b612eaf1fa6dffffb98e4395` (push run `36126817873`, both jobs success; `yarn test` 1346 in 37 files, `yarn test:db` 340 in 9 files, `smoke:p4b` 64 checks, lint "Found 0 warnings and 0 errors.", entry chunk 319.34 kB with no bundle advisory); `feature/p4c-correspondence` at creation, the same commit (push run `36129072727`, both jobs success). Accepted R9-final head `a31ad90` (push run `36120427855`; pull_request run `36123954319`; both success). R9 remediation on `feature/r9-fact-support-readback`: the code head `b862cf7b560b61dd5e1586feb0f0b9b75290bedb` (push run `36116320128`, both jobs success; `yarn test` 1346 in 37 files, `yarn test:db` 340 in 9 files, `smoke:p4b` 64 checks with the read-back, lint "Found 0 warnings and 0 errors.", entry chunk 319.34 kB with no bundle advisory); the branch at creation `eb83b19` (push run `36113092678`). `main` after the P4B merge: `eb83b19d224c0c7e6bf1bd7de5c21bd3c2e0c361` (push run `36106838217`, both jobs success). P4B R9 submission head `6ff6775` (push run `36105169096`; pull_request run `36106823445`; both success). P4B on `feature/p4b-case-intake`: the code head `35cf55277a72e35acafb26a636f4d631adf594c7` (push run `36104287781`, both jobs success; `yarn test` 1326 in 36 files, `yarn test:db` 333 in 9 files, `smoke:p4b` 54 checks, lint "Found 0 warnings and 0 errors.", entry chunk 319.26 kB with no bundle advisory); the UI with route-level code splitting `aa9ee2c` (push run `36099915631`); the API with `smoke:p4b` `e2ce11d` (push run `36097731802`, `smoke:p4b` 54 checks); the checkpoint `e77f809` (push run `36095162287`). `main` after the P4A merge: `1d91c41ec480aed3579122e86485df1e73fbb741` (push run `36094428857`, both jobs success). Accepted P4A closeout head `602f0da` (push run `36093677700`; pull_request run `36094064653`; both success). R8 final submission head `eeb94ce573dad9b2da71e01922f1db02e734437a` (documentation on `ab56139`; push run `36084981272`, both jobs success). R8 remediation code head on `feature/p4a-case-core`: `ab56139a1267e6ad28a68abc1cba82bf477872cc` (push run `36083151963`, both jobs success; `yarn test` 1247, `yarn test:db` 301, `smoke:p4a` 50 checks with the read-back and its cross-case refusal, lint "Found 0 warnings and 0 errors."); before the browser fix F4: `e745a20` (push run `36082079759`, success). P4A R8 submission head `7a11ce4` (push run `36030891042`, both jobs success). P4A code head: `61164fab2b22b4a46462714f8d86b00adef4e77b` (push run `36028683469`, both jobs success, including the new compiled `smoke:p4a`, 47 checks). Lint fix `d615297` (push run `36016274650`, "Found 0 warnings and 0 errors."). `main` after the P3B merge: `3649bef4d83feeec3bcf6b8293757354af739ae4` (push run `36008448885`, both jobs success). Accepted P3B head: `05ce7b06118182d6592a434a90f4b88ce53f6fa8` (push run `36004964689`, pull_request run `36006237155`, both success). R7 remediation: `ee31fa38bf398a56be0bf703409128cb0d6ef5b2` (push run `36002633201`, both jobs success); R7 submission head `8e513f0` (run `35991749458`, success). P3B code head: `04b852863705da8115c7d165eb852768afe3c976` (push run `35990960584`, both jobs success); P3B API + `smoke:p3b`: `f79aa0a` (run `35980464772`). `main` after the P3A merge: `adea2bc5b7ab5ed4cbc9fb309055b6f930d64cce` (push run `35968171532`, both jobs success). Accepted P3A head: `c1b8d22cc748d1b0be10be0ecf4a5f464d2c396e` (push run `35966993063`, pull_request run `35966987256`, both success). P3A code: `85c2839` (push run `35966162924`). P2 R5 remediation: `4b67fff631fa8b51a243fbecd64d43f76ce79d97` (run `35953912083`). P2 at R5: `cbfd640` (run `35948987435`). P1.1: `8fe96ae` (run `35894704102`). P1: `5507726` (run `35885393358`). P0: `0dfc7a5` (run `35863416044`) |
| Initial migration | `20260923103912_initial_schema`, sha256 `b54c36fdaada7bdd31e08558a93d70e9e40fd4c48348681c9c2a124503426515` — still the only migration (P1, P1.1, P2, the R5 remediation, P3A, P3B, the R7 remediation, P4A, P4B, the R9 remediation, P4C and P4D made no schema change) |
| Contract baseline | Active wire contract **`TB-SCHEMA-API-v1.2.0`** since R9 final (ADR-0005, **ACCEPTED**, operator, 2026-09-25; merged by pull request #5) = `TB-SCHEMA-API-v1.1.0` (ADR-0004, ACCEPTED at R8 final) + the additive amendment `docs/contracts/TB-SCHEMA-API-v1.2.0/amendment.json` (sha256 `b5cae658…c294`): one read `getCaseFactSources` (GET `/cases/{caseId}/facts/{id}/sources`) and two schemas (`CaseFactSourcesView`, `GetCaseFactSourcesResponse`), OpenAPI `info.version` 1.2.0 — 288 schemas, 143 operations. `TB-SCHEMA-API-v1.1.0` = the frozen `TB-SCHEMA-API-v1.0.0` reference (the frozen historical reference, unchanged under `docs/reference`) + `docs/contracts/TB-SCHEMA-API-v1.1.0/amendment.json` (sha256 `2f4df697…dfda85`): one read `getCaseAuthoritySelection` (GET `/cases/{caseId}/authority-selections/{id}`) and two schemas — 286 schemas, 142 operations. Every earlier schema and operation stays byte-identical; `tests/contracts/release.ts` composes the releases in order and each earlier release is still reproduced to the digests recorded at its acceptance; both release records are pinned and never edited. PFC wire id `PFC-YT-EMAIL-v1.1` (unchanged); active Zod source per ADR-0002 (ACCEPTED); `CONTRACT_BASELINE` = `TB-SCHEMA-API-v1.2.0`. The opt-in `yarn test:transition-baseline` fails by design since the v1.1.0 edit (a historical oracle, never a gate). `AppMeta.schemaRelease` stays `TB-SCHEMA-API-v1.0.0` as recorded until a separately approved future contract decision (ADR-0004, ADR-0005). P2 implements 36 directory operations; P3A adds the 4 directory `bindCanonical*` operations (deferred by D2 until sources existed), the 4 source operations and the 9 route operations; P3B adds the 23 representation-authority operations — 76; P4A adds the 16 case operations (Case 10, CaseSource 4, CaseAuthoritySelection 2) — 92; the R8 remediation adds `getCaseAuthoritySelection` — 93; P4B adds the 22 case intake operations (ReportedItem 6, CaseWork 6, UseMapping 6, CaseFact 4) with no wire change — 115; the R9 remediation adds `getCaseFactSources` — 116; P4C adds the 5 correspondence operations (`listCorrespondence`, `captureCorrespondence`, `getCorrespondence`, `bindCaseCorrespondence`, `listCaseCorrespondenceBindings`) with no wire change — **121 business operations routed** on `main` (since the P4C merge `ed5b3d7`); P4D adds the read `getProductionContext` with no wire change — **122** on `feature/p4d-production-context`; every later case operation (prompts onward) is not routed. The contract gaps reported at R8 (`P4A_CASE_CORE.md` §9) and R9 (`P4B_CASE_INTAKE.md` §1.2, §19) are resolved (`P4A_CASE_CORE.md` §21; `P4B_CASE_INTAKE.md` §25–§26) |
| Database image | `mysql:8.4.11@sha256:0744ee5ef89ce6ccfa13de3e579fe6b9e27f93dd70da9c06d2c908b1b193fb8d` on `127.0.0.1:3307` |
| Seed | Disabled synthetic actor `00000000-0000-4000-8000-00000000a0c7`; canonical digest `0ee26dc3ee5f3a1030fb78a6cadaf4dabef1f5a296b58163248584e1e0bfb775` (first PC = CI; unchanged by P1) |
| Toolchain | Node 24.21.0, Yarn 4.18.0, TypeScript 7.0.2, Prisma 7.10.0, Zod 4.6.5, ajv 8.20.0, ajv-formats 3.0.1; P1 adds argon2 0.45.1, react-router 8.4.0, @nestjs/testing 12.0.4, happy-dom 20.14.5 (see `CLAUDE.md`); P2, P3A, P3B, the R7 remediation, P4A, P4B, the R9 remediation, P4C and P4D add no dependency (lockfile unchanged) |
| Local env | Root `.env` now also needs `TB_SESSION_SECRET` and `TB_ALLOWED_WEB_ORIGINS`; `yarn env:init` adds missing P1 keys (and replaces empty/placeholder P1 values) without touching any other line (first PC: done 2026-09-23) |
| Decisions | ADR-0001 manual MySQL migration semantics (ACCEPTED); ADR-0002 Zod-first contract authoring (ACCEPTED); ADR-0003 single-PC development baseline (ACCEPTED, operator, 2026-09-24); ADR-0004 TB-SCHEMA-API-v1.1.0 additive case-authority-selection read (ACCEPTED, operator, 2026-09-25, R8 final); ADR-0005 TB-SCHEMA-API-v1.2.0 additive case-fact-sources read (ACCEPTED, operator, 2026-09-25, R9 final); P1 interpretations listed in `P1_AUTH_SHELL.md` §4 (accepted at R4); P2 interpretations in `P2_DIRECTORY.md` §12 (decided at R5); P3A interpretations in `P3A_SOURCES_ROUTE.md` §16, accepted at R6 (§19); P3B interpretations in `P3B_REPRESENTATION_AUTHORITY.md` §17, accepted at R7 (§21.1; R7 closed as PASS, §22); P4A interpretations in `P4A_CASE_CORE.md` §17, submitted at R8 (R8: PASS_WITH_ONE_CONTRACT_REMEDIATION; remediation §21; R8 final **PASS**, §22); P4B interpretations in `P4B_CASE_INTAKE.md` §20, submitted at R9 (R9: PASS_WITH_ONE_CONTRACT_REMEDIATION; remediation §25; R9 final **PASS**, §26); P4C interpretations in `P4C_CORRESPONDENCE.md` §25 (with the design decisions D1–D20, §1.3), submitted at R10; R10 = **PASS** with no remediation (§29); P4D interpretations in `P4D_PRODUCTION_CONTEXT.md` §25 (with the design decisions D1–D6, §1.3), submitted at R11 (PENDING) |

## P0 status (see `docs/verification/p0/P0_FINAL_REPORT.md`)

| Scope | Status |
|---|---|
| P0_FIRST_PC | PASS |
| P0_CI | PASS (runs 35863416044 @ `0dfc7a5`, 35864210436 @ `f6f5a96`; baseline run 35873152877 @ `b9eea37`) |
| P0_WINDOWS_BROWSER | PASS — OPERATOR_REPORTED (first PC, reported 2026-09-23; not automated evidence) |
| P0_SECOND_PC | DEFERRED_BY_OPERATOR (ADR-0003, 2026-09-24; never run, not passed) |
| P0_TWO_PC_ACCEPTANCE | NOT_COMPLETED (P0-26, P0-27) |
| P0_SINGLE_PC_BASELINE | VERIFIED (home PC + CI) |
| P0_OVERALL | NOT_COMPLETE against the original two-PC acceptance contract; development continues on the verified single-PC baseline |

## P1 status (see `docs/verification/p1/P1_AUTH_SHELL.md`, `P1_1_AUTH_RECOVERY.md`)

| Scope | Status |
|---|---|
| P1_FIRST_PC | PASS (automated) |
| P1_CI | PASS (run 35885393358 @ `5507726`) |
| R4 review | PASS_WITH_NOTES (operator, 2026-09-23) |
| P1_1_FIRST_PC | PASS (automated); the two port-bound checks left NOT RUN locally in the P1.1 record: PASS — OPERATOR_REPORTED (see below) |
| P1_1_CI | PASS (run 35894704102 @ `8fe96ae`, both jobs success) |
| R4.1 review | ACCEPTED (operator, 2026-09-24) |
| P1_WINDOWS_BROWSER | NOT_RUN (not reported by the operator; never inferred from tests or database traces) |

Operator report at R4.1 acceptance (2026-09-24), recorded as stated: "the remaining first-PC verification checks have passed". The report does not name the commands, the commit or any output. The P1.1 record had left `yarn smoke:local` and `yarn dev:verify-shutdown` NOT RUN locally because the operator's own `yarn dev` held ports 3000/5173. This is OPERATOR_REPORTED, not automated evidence, and it is not read as a Windows-browser sign-in report.

## P2 status (see `docs/verification/p2/P2_DIRECTORY.md`)

| Scope | Status |
|---|---|
| P2_FIRST_PC | PASS (automated; R5 closeout sweep repeated after the remediation) |
| P2_CI | PASS for `cbfd640` (run 35948987435), `a885b96` (run 35950151963) and the remediation `4b67fff` (run 35953912083); the closeout documentation run is reported with the closeout |
| P2_BROWSER | PASS 14/14 at R5, plus the identity-lock scenarios re-run after the remediation — Playwright MCP isolated browser against the compiled API on the disposable `tb_notice_test` (`yarn ui:sandbox`); supplemental to the test suites |
| R5 review | PASS_WITH_ONE_REMEDIATION (operator, 2026-09-24), then **PASS / CLOSED** with the remediation accepted (operator, P3A mission, 2026-09-24) |
| P2_STATUS | VERIFIED_COMPLETE_FOR_CURRENT_SCOPE (operator) |

## P3A status (see `docs/verification/p3a/P3A_SOURCES_ROUTE.md`)

| Scope | Status |
|---|---|
| P3A_FIRST_PC | PASS (automated: `yarn test` 1093, `yarn test:db` 190, full regression sweep with drift checks) |
| P3A_CI | PASS for `73fa223` (run 35962967984), `9481bd0` (run 35963513064), the code head `85c2839` (push run 35966162924; pull_request run 35966165567) and the accepted head `c1b8d22` (push run 35966993063; pull_request run 35966987256). The failed checks of `afeb012` (run 35959934247) and `0c289be` (run 35961207473) — a stale `smoke:local` expectation, fixed in `73fa223` — are preserved as historical evidence; no history was rewritten |
| P3A_BROWSER | PASS 21/21 — Playwright MCP isolated browser against the compiled API on the disposable `tb_notice_test` (`yarn ui:sandbox`); one copy defect found and fixed (`85c2839`); supplemental to the test suites |
| P3A_NEGATIVE_CONTROLS | PASS 28/28 (every disabled protection failed its responsible suites; files restored byte-identically) |
| R6 review | **PASS** (operator, 2026-09-24); the interpretations were accepted (see *R6 decisions* above) |
| P3A_STATUS | VERIFIED_COMPLETE |
| P3A_MERGE | MERGED_TO_MAIN — pull request #1, GitHub merge commit `adea2bc` (method: merge commit, not squash or rebase), merged 2026-09-24T07:10:08Z; `main` push CI run 35968171532 success |

R7 remediation (`ee31fa3`, on `main` since the P3B merge `3649bef`): SourceReference `observedAt`/`reviewedAt` are stored as exactly the supplied instant or refused with 422 before any write — see `P3B_REPRESENTATION_AUTHORITY.md` §21.

Recorded boundaries: a SourceReference is metadata and a pointer, not evidence proof; a canonical binding is an identity/reference binding, not authority; a Route link is not authority; P3A contains no Mandate, MandateVersion, MandateCoverage, CoverageSigner or AuthorityEvent (P3B) and no case data. Session-row and idempotency-record retention cleanup remain DEFERRED and non-blocking (unchanged).

## P3B status (see `docs/verification/p3b/P3B_REPRESENTATION_AUTHORITY.md`)

| Scope | Status |
|---|---|
| P3B_FIRST_PC | PASS (automated: `yarn test` 1148, `yarn test:db` 251, full regression sweep with drift checks) |
| P3B_CI | PASS for `f79aa0a` (run 35980464772), the code head `04b8528` (push run 35990960584; both jobs success, including the new compiled `smoke:p3b`, 36 checks), the R7 submission head `8e513f0` (run 35991749458), the remediation `ee31fa3` (run 36002633201) and the accepted head `05ce7b0` (push run 36004964689; pull_request run 36006237155). The branch-creation run 35970356174 (`adea2bc`) was cancelled by the concurrency group when `c8da59f` was pushed; it stays as it is |
| P3B_BROWSER | PASS 20/20 — Playwright MCP isolated browser against the compiled API on the disposable `tb_notice_test` (`yarn ui:sandbox`); three UI findings found and fixed (`04b8528`); supplemental to the test suites |
| P3B_NEGATIVE_CONTROLS | PASS 51/51 (every disabled protection failed its responsible suites; files restored byte-identically; `tb_notice_test` empty after every DB control; the timing-dependent concurrent-freeze control detected 3/3) |
| R7 review | **PASS** (operator, 2026-09-24) — first **PASS_WITH_ONE_REMEDIATION** (functionally accepted; decisions accepted, see *R7 decisions*), closed as PASS after the remediation |
| P3B_R7_REMEDIATION | **IMPLEMENTED AND VERIFIED**, accepted at the closeout — SourceReference instant storability, one shared rule with P3B (`ee31fa3`): tests (`yarn test` 1196, `yarn test:db` 256), negative controls 8/8, regression sweep 19/19, targeted browser re-check 7/7, CI run 36002633201 success (`P3B_REPRESENTATION_AUTHORITY.md` §21) |
| P3B_STATUS | VERIFIED_COMPLETE |
| P3B_MERGE | MERGED_TO_MAIN — pull request #2, GitHub merge commit `3649bef` (method: merge commit, not squash or rebase), merged 2026-09-24T13:50:21Z; `main` push CI run 36008448885 success (`P3B_REPRESENTATION_AUTHORITY.md` §22) |

Recorded boundaries (persistent, see `CLAUDE.md`; carried forward at the closeout, §22.3): authority records are not self-proving; a Mandate is a representation container, not a legal identity; a frozen version is an immutable snapshot, not completeness, human or legal approval, current authority or G1 PASS; Coverage is not G1; CoverageSigner is not G7 and a User is never a Signer; an AuthorityEvent is not proof by existence; `Route.preferredCoverageId` is an operational preference only; citations are pinned to exact source revisions; no authority currentness is inferred from dates or silence; DOCUMENT_REVIEWED is never inferred; dates and instants that cannot round-trip exactly are refused before persistence. No case data exists; Case operations are not routed.

## P4A status (see `docs/verification/p4a/P4A_CASE_CORE.md`)

| Scope | Status |
|---|---|
| P4A_FIRST_PC | PASS (automated: `yarn test` 1228 in 32 files, `yarn test:db` 295 in 8 files, full regression sweep with drift checks; lint **0 warnings**). R8 remediation: `yarn test` 1247 in 33 files, `yarn test:db` 301 in 8 files, sweep 21/21 on `ab56139`, lint 0 warnings |
| P4A_CI | PASS for the code head `61164fa` (push run 36028683469; both jobs success, including the new compiled `smoke:p4a`, 47 checks), the R8 submission head `7a11ce4` (push run 36030891042), the R8 remediation heads `e745a20` (push run 36082079759) and `ab56139` (push run 36083151963; `smoke:p4a` 50 checks) and the R8 final submission head `eeb94ce` (push run 36084981272, both jobs success). The run of the closeout head is reported with the merge |
| P4A_BROWSER | PASS 23/23 — Playwright MCP isolated browser against the compiled API on the disposable `tb_notice_test` (`yarn ui:sandbox`); three UI findings found and fixed (F1 route-dialog focus, F2 hidden label, F3 horizontal scroll at 390px); R8 read-back PASS 12/12 with one layout finding found and fixed (F4, `ab56139`); supplemental to the test suites |
| P4A_NEGATIVE_CONTROLS | PASS 24/24 (every disabled protection failed its responsible tests; files restored byte-identically; working tree identical before and after; `tb_notice_test` empty afterwards); R8 remediation 17/17 and the P4A 24 again 24/24 on `ab56139` |
| R8 review | **PASS** (operator, 2026-09-25, R8 final) — first **PASS_WITH_ONE_CONTRACT_REMEDIATION** (implementation, tests, browser verification, negative controls and CI accepted); the one remediation (selection read-back, TB-SCHEMA-API-v1.1.0) was **IMPLEMENTED AND VERIFIED** (`P4A_CASE_CORE.md` §21) and accepted with ADR-0004 (§22) |
| P4A_STATUS | **VERIFIED_COMPLETE** |
| P4A_MERGE | **MERGED_TO_MAIN** — pull request #3, GitHub merge commit `1d91c41` (method: merge commit, not squash or rebase), merged 2026-09-25T04:26:08Z; `main` push CI run 36094428857 success (`P4A_CASE_CORE.md` §23) |

Recorded boundaries (persistent, see `CLAUDE.md`): case isolation — nothing of one case supports, appears in or carries over to another, and a case-scoped source applies only to the cases it names; a CaseSource link is an association, not proof, and never changes provenance; CaseAuthoritySelection is the authority chain selected/pinned for evaluation in one specific Case — not G1 PASS, current authority, validity, signer eligibility, G7 or readiness; a selection pins exact records (the case's route, one signer, coverages of frozen versions recording that signer, the basis source revision), is append-only and never follows newer records; its read-back (`getCaseAuthoritySelection`, TB-SCHEMA-API-v1.1.0) returns exactly the stored selection and pinned rows of that case, never another case's and never anything recomputed from present-day state; nothing is selected implicitly or inherited across cases; P4A computes no readiness. No real case data exists; every later case operation is unrouted.

## P4B status (see `docs/verification/p4b/P4B_CASE_INTAKE.md`)

| Scope | Status |
|---|---|
| P4B_FIRST_PC | PASS (automated: `yarn test` 1326 in 36 files, `yarn test:db` 333 in 9 files, full regression sweep 21/21 with drift checks on `35cf552`; lint **0 warnings**). R9 remediation: `yarn test` 1346 in 37 files, `yarn test:db` 340 in 9 files, sweep 21/21 on `b862cf7`, lint 0 warnings |
| P4B_CI | PASS for `e2ce11d` (push run 36097731802; the new compiled `smoke:p4b`, 54 checks), `aa9ee2c` (push run 36099915631), the code head `35cf552` (push run 36104287781; both jobs success, `smoke:p4b` 54 checks) and the R9 submission head `6ff6775` (push run 36105169096; pull_request run 36106823445); post-merge `main` `eb83b19` (push run 36106838217); R9 remediation code head `b862cf7` (push run 36116320128; `smoke:p4b` 64 checks); the accepted R9-final head `a31ad90` (push run 36120427855; pull_request run 36123954319) and post-merge `main` `d2b6f00` (push run 36126817873), all success |
| P4B_BROWSER | PASS 27/27 — Playwright MCP isolated browser against the compiled API on the disposable `tb_notice_test` (`yarn ui:sandbox`); scenario 15's reading-back of fact supports BLOCKED by the reported contract gap (the page said so); one UI finding found and fixed (F1 "Load latest version", `f952b73`); R9 remediation read-back PASS 12/12 (the blocked part verified); supplemental to the test suites |
| P4B_NEGATIVE_CONTROLS | PASS 63/63 (every disabled protection failed its responsible tests; files restored byte-identically; working tree identical before and after; `tb_notice_test` empty afterwards); R9 remediation 21/21 on `b862cf7` |
| P4B_CONTRACT | No wire change in P4B (TB-SCHEMA-API-v1.1.0 active then); FactSource read-back gap REPORTED, not worked around — remediated by the additive release TB-SCHEMA-API-v1.2.0 (R9 remediation; ADR-0005 **ACCEPTED** at R9 final, the active contract since) |
| P4B_DATABASE | No migration, no schema change (the R9 remediation made none either) |
| R9 review | **PASS** (operator, 2026-09-25, R9 final) — first **PASS_WITH_ONE_CONTRACT_REMEDIATION** (P4B functionally accepted); the one remediation (fact-support read-back, TB-SCHEMA-API-v1.2.0) was **IMPLEMENTED AND VERIFIED** (`P4B_CASE_INTAKE.md` §25) and accepted with ADR-0005 (§26) |
| P4B_STATUS | **VERIFIED_COMPLETE** (R9 final; at R9: VERIFIED_COMPLETE_FOR_IMPLEMENTED_SCOPE — the 22 contracted intake operations) |
| P4B_MERGE | **MERGED_TO_MAIN** — pull request #4, GitHub merge commit `eb83b19` (method: merge commit), merged by the operator 2026-09-25T07:16:46Z; `main` push CI run 36106838217 success. The R9 remediation: pull request #5, GitHub merge commit `d2b6f00` (method: merge commit), merged by the operator 2026-09-25T10:58:02Z; `main` push CI run 36126817873 success (`P4B_CASE_INTAKE.md` §26) |

Recorded boundaries (persistent, see `CLAUDE.md`): a ReportedItem is not an infringement finding; a CaseWork is not ownership proof; a UseMapping is not an infringement finding; a CaseFact is explicit, case-specific structured information with its provenance as supplied; permission is never inferred from silence and similarity alone is never infringement; intake records and facts never cross Case boundaries; revisions preserve history; sources and provenance are pinned and never upgraded; P4B computes no readiness. R9 remediation (accepted at R9 final; TB-SCHEMA-API-v1.2.0): the supports of one fact revision are read back exactly as recorded — never merged across revisions, never hidden after a pause or unlink, never following a newer source revision, and never proof, a review, a finding, G1–G7 or readiness. No real case data exists; every later case operation is unrouted.

## P4C status (see `docs/verification/p4c/P4C_CORRESPONDENCE.md`)

| Scope | Status |
|---|---|
| P4C_FIRST_PC | PASS (automated: `yarn test` 1377 in 39 files, `yarn test:db` 380 in 10 files, full regression sweep 21/21 with drift checks on `b9fcaf2`; lint **0 warnings**) |
| P4C_CI | PASS for the branch at creation `d2b6f00` (push run 36129072727), the R9 closeout checkpoint `abad3ee` (push run 36129721720), the API `48ceebd` (push run 36133062817; the new compiled `smoke:p4c`, 62 checks), the UI `0038c56` (push run 36135445880), the code head `b9fcaf2` (push run 36139089683), the R10 submission head `1b2e27b` (push run 36140515842), the accepted head `d6cd152` (push run 36141175731), the closeout head `edebb80` (push run 36151228476; pull_request run 36152011037) and post-merge `main` `ed5b3d7` (push run 36153127772), all success, both jobs |
| P4C_BROWSER | PASS 29/29 — Playwright MCP isolated browser against the compiled API on the disposable `tb_notice_test` (`yarn ui:sandbox`); no finding; teardown verified; supplemental to the test suites |
| P4C_NEGATIVE_CONTROLS | PASS 38/38 in the final run (all 21 control kinds of mission §36 plus one extra; every disabled protection failed its responsible tests; files restored byte-identically; working tree identical before and after; `tb_notice_test` empty afterwards); run 1 36/37 — one control defect, fixed in the runner and kept in the history |
| P4C_CONTRACT | No wire change — TB-SCHEMA-API-v1.2.0 stays active; no contract gap found |
| P4C_DATABASE | No migration, no schema change |
| R10 review | **PASS** (operator, 2026-09-25) — no remediation (`P4C_CORRESPONDENCE.md` §29) |
| P4C_STATUS | **VERIFIED_COMPLETE** (the five contracted correspondence operations) |
| P4C_MERGE | **MERGED_TO_MAIN** — pull request #6, GitHub merge commit `ed5b3d7` (method: merge commit, not squash or rebase), merged 2026-09-25T15:17:05Z; `main` push CI run 36153127772 success (`P4C_CORRESPONDENCE.md` §30) |

Recorded boundaries (persistent, see `CLAUDE.md`):

- capture ≠ send;
- OUTBOUND ≠ AS_SENT;
- an event type names a captured past event, never a command;
- OPERATOR_REPORTED never becomes raw-source verification;
- Message-ID and subject are not identity (no deduplication; `sourceIdentityHash` null);
- one message with several bindings is not several transmissions;
- an outcome is item-specific in V1;
- silence is never an outcome;
- supersession corrects without altering history;
- captured content is untrusted and shown as plain text;
- zero external actions (no mailbox, send, reply, read-marking, contact, submission or Drive write);
- P4C computes no readiness.

No real correspondence or case data exists.

## P4D status (see `docs/verification/p4d/P4D_PRODUCTION_CONTEXT.md`)

| Scope | Status |
|---|---|
| P4D_FIRST_PC | PASS (automated: `yarn test` 1397 in 41 files, `yarn test:db` 422 in 11 files — P4D 42 DB, 12 unit, 8 web; full regression sweep 21/21 steps exit 0 on `1b012bd`; lint **0 warnings**) |
| P4D_SNAPSHOT | PASS — one REPEATABLE READ snapshot per read; commits made while a read is in flight are not mixed in (DB test; negative control NC-P4D-16 caught) |
| P4D_CI | PASS for the API `75f07c2` (push run 36160776299), the compiled smoke `c7df48b` (push run 36162478106; `smoke:p4d` 88 checks) and the keyboard fix `6bbba3a` (push run 36165581902), all success, both jobs; the R11 submission head's run is reported at R11 |
| P4D_BROWSER | PASS 32/32 — Playwright MCP isolated browser against the compiled API on the disposable `tb_notice_test` (`yarn ui:sandbox`); one finding (keyboard focus after a requested read) fixed in `6bbba3a` and re-verified; teardown verified after both sessions |
| P4D_NEGATIVE_CONTROLS | PASS 22/22 in the final run on `1b012bd` (all 19 control kinds of mission §49, three with a UI variant; every disabled protection failed its responsible tests on their own assertions; files restored byte-identically; working tree identical before and after; `tb_notice_test` empty afterwards); run 1 21/22 — one test weakness (a claim scan over `textContent`), fixed in `1b012bd` and kept in the history |
| P4D_CONTRACT | No wire change — TB-SCHEMA-API-v1.2.0 stays active; `getProductionContext` used exactly as contracted; no contract gap found |
| P4D_DATABASE | No migration, no schema change, no persistence for the read |
| R11 review | **PENDING** — no pull request, no merge |

Recorded boundaries (persistent, see `CLAUDE.md`):

- context = recorded input, not a legal conclusion;
- exact case isolation;
- exact authority pinning;
- no latest/default substitution;
- MISSING stays missing; CONFLICT stays conflict;
- correspondence posture stays visible; `*_AS_SENT` does not imply a verified package;
- one consistent snapshot per read;
- a complete dependency closure; the digest is never merely `Case.rowVersion`;
- no readiness, no writes, no external action.

Observations for R11 (not changed): the P4A documentation says the intake label does not move `contextRevision` while the accepted implementation and test do (P4D follows the implementation); 18 claim scans of the P3A–P4C web tests read `textContent` (the weakness P4D's controls found in its own scans); the page's source citations are fetched one by one without a cache.

No real case, owner, authority or correspondence data exists.

## Next phases (not started)

| Phase | Scope | Gate |
|---|---|---|
| Prompt generation (`generatePrompt`, PromptSnapshot) — **not authorized**; proposed after R11 (`P4D_PRODUCTION_CONTEXT.md` §28) | A prompt built from one exact production context (its digest and dependencies pinned). No readiness, G1–G7 decision, READY_FOR_SIGNER, signature or external action; there is no AI-provider call. Candidates, validation, assessments, readiness and unsigned export stay later | its own review gate |

**CaseAuthoritySelection (semantic note, operator, recorded for P4A and implemented there):** "the authority chain selected/pinned for evaluation in this specific Case". It does not mean G1 PASS, current authority confirmed, legally valid authority adjudicated, signer eligibility confirmed, G7 or notice readiness; actual G1 evaluation is a later readiness phase.

Everything else case-specific (prompts, candidates, validation, assessments, readiness, unsigned export) remains later. Each phase needs an explicit, approved mission.

## Remaining requirements

1. P0: the second-PC reproduction is deferred by the operator (ADR-0003). If a workstation is added later, it runs `docs/architecture/SECOND_PC_REPRODUCTION_RUNBOOK_v1.md`; only that evidence can change `P0_SECOND_PC` or `P0_TWO_PC_ACCEPTANCE`.
2. P4A: R8 final = **PASS**, ADR-0004 accepted (operator, 2026-09-25) and merged to `main` (PR #3, merge commit `1d91c41`, `main` CI green). P4B: R9 = PASS_WITH_ONE_CONTRACT_REMEDIATION, then **PASS** at R9 final (operator, 2026-09-25); P4B merged (PR #4, merge commit `eb83b19`) and the remediation (TB-SCHEMA-API-v1.2.0, `getCaseFactSources`) merged (PR #5, merge commit `d2b6f00`), `main` CI green; ADR-0005 accepted. P4C: R10 = **PASS** (operator, 2026-09-25), P4C VERIFIED_COMPLETE and merged to `main` (PR #6, merge commit `ed5b3d7`, `main` CI green). P4D (production context) is implemented on `feature/p4d-production-context` and submitted for R11 (PENDING; no pull request, not merged); every phase after P4D needs a new mission.
3. A manual Windows-browser sign-in is recorded as `P1_WINDOWS_BROWSER = PASS — OPERATOR_REPORTED` only when the operator reports it.
4. Resolved: the lint warning reported since `ee31fa3` was fixed in `d615297` (first commit of the P4A mission); `yarn lint` reports 0 warnings (CI run 36016274650: "Found 0 warnings and 0 errors.").
5. Non-blocking: `AppMeta.schemaRelease` of the unrouted `GET /meta` stays `TB-SCHEMA-API-v1.0.0` as recorded until a separately approved future contract decision (changing it is not additive — ADR-0004); the web bundle advisory is resolved in P4B by route-level lazy loading (one 552.78 kB chunk at `ab56139`, 635.01 kB with the P4B UI; since `aa9ee2c` an entry of 319.26 kB plus page chunks, no advisory — `P4B_CASE_INTAKE.md` §13); the threshold was never raised. Resolved at the R8 remediation: the CaseAuthorityCoverage read-back gap (§9 → §21). Resolved at R8 final: ADR-0004 acceptance. Resolved at R9 final: the FactSource read-back gap and ADR-0005 acceptance (`P4B_CASE_INTAKE.md` §25–§26). Open: the P4A "Link a source" page keeps its conflict notice after "Load latest version" (the F1 pattern, fixed for the P4B pages only; not touched by the R9 remediation).
