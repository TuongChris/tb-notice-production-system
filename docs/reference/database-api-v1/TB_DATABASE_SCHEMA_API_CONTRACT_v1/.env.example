# Example only. Never use production credentials in the two local development databases.
DATABASE_URL="mysql://tb_dev:REPLACE_WITH_LOCAL_PASSWORD@127.0.0.1:3306/tb_dev"
# MySQL8.4 local instance required; this file does not provision it.
